---
phase: 09-typography-foundation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - components/app-header.tsx
  - components/message.tsx
  - components/confidence-check.tsx
  - components/concept-node.tsx
  - components/learning-journal.tsx
autonomous: true

must_haves:
  truths:
    - "Concept map node labels render in Courier Prime (monospace), visually distinct from surrounding serif text"
    - "Conversation messages (both user and assistant) render in Libre Baskerville (serif)"
    - "Mode badges (Live/Replay) and role labels (Claude/You) render in Courier Prime with visible letter-spacing"
    - "Learning journal heading and entry numbers use monospace font with uppercase letter-spacing"
    - "Assessment badges (tracking/partial/confused) render in monospace with letter-spacing"
  artifacts:
    - path: "components/concept-node.tsx"
      provides: "Concept map nodes with mono font for labels"
      contains: "font-mono"
    - path: "components/message.tsx"
      provides: "Conversation messages with serif font and role labels in mono"
      contains: "type-label"
    - path: "components/app-header.tsx"
      provides: "Header with serif app name, mono mode badges"
      contains: "font-mono"
    - path: "components/confidence-check.tsx"
      provides: "Confidence check with mono assessment badges"
      contains: "font-mono"
    - path: "components/learning-journal.tsx"
      provides: "Journal with mono heading and entry numbers"
      contains: "type-label"
  key_links:
    - from: "components/message.tsx"
      to: "app/globals.css"
      via: "type-label CSS class from Plan 01"
      pattern: "type-label"
    - from: "components/concept-node.tsx"
      to: "app/globals.css"
      via: "font-mono CSS variable from Plan 01"
      pattern: "font-mono"
---

<objective>
Apply the typography foundation from Plan 01 across all UI components. Concept map nodes get Courier Prime (mono) for a tag/identifier feel. Conversation messages stay in Libre Baskerville (serif) for warmth. Labels, badges, and role indicators get mono treatment with letter-spacing.

Purpose: The font loading from Plan 01 makes the fonts available globally, but components need explicit font assignments to create the serif/mono distinction that gives ThreadTutor its typographic identity.
Output: All five core component files updated with appropriate font family, weight, and spacing classes.
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-typography-foundation/09-01-SUMMARY.md
@components/app-header.tsx
@components/message.tsx
@components/confidence-check.tsx
@components/concept-node.tsx
@components/learning-journal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply typography to header, messages, and confidence checks</name>
  <files>
    components/app-header.tsx
    components/message.tsx
    components/confidence-check.tsx
  </files>
  <action>
**app-header.tsx:**
- App name "ThreadTutor": keep serif (inherits from body), change from `font-semibold` to `font-bold` for h1-like emphasis
- Tagline "Socratic learning with AI": keep serif, keep subtle styling
- Topic breadcrumb: keep serif (it is content text)
- Mode badges (Live/Replay): add `font-mono tracking-wider uppercase` to make them feel like status indicators. Change from `text-xs` to `text-[0.6875rem]` for slightly more presence. Add `tracking-[0.1em]` for letter-spacing
- The `/` separator: no change needed

**message.tsx:**
- Role labels ("Claude" / "You"): replace the existing classes with `type-label` utility class from globals.css (mono, 0.75rem, uppercase, 0.12em letter-spacing). Keep the color classes (`text-indigo-400` for Claude, `text-zinc-500` for You)
- Message body text: inherits serif from body. Ensure the prose classes on assistant messages include comfortable reading: add `prose-p:leading-[1.7]` to the prose class list for explicit line-height. Keep existing prose modifiers
- User message text: inherits serif from body, no change needed beyond ensuring `text-base` stays
- The `border-b border-zinc-800` message separators: keep as-is (phase context says "conversation messages flow together with minimal separation")

**confidence-check.tsx:**
- Question text: keep serif (inherits from body), keep `text-base font-medium`
- Assessment badges (tracking/partial/confused): add `font-mono tracking-wider uppercase text-[0.6875rem]` to make them feel like status tags. Replace `text-xs font-medium` with this treatment
- Feedback text: keep serif, keep subtle styling
- Input placeholder and button: serif for input text (inherits), mono for the "Send" button text (add `font-mono tracking-wide uppercase text-xs`). The button is an action element that benefits from mono treatment
  </action>
  <verify>Run `npm run build` -- no errors. Inspect all three files for correct font-mono, type-label, and tracking class usage.</verify>
  <done>Header mode badges are mono+uppercase+tracked. Message role labels use type-label class. Assessment badges are mono+uppercase+tracked. Send button is mono+uppercase. All body/message text inherits serif.</done>
</task>

<task type="auto">
  <name>Task 2: Apply typography to concept map nodes and learning journal</name>
  <files>
    components/concept-node.tsx
    components/learning-journal.tsx
  </files>
  <action>
**concept-node.tsx:**
- Node label text: change from `text-base font-medium text-zinc-200` to `font-mono text-sm tracking-wide text-zinc-200`. This makes concept labels feel like tags/identifiers, distinct from the surrounding serif conversation text. Use `text-sm` instead of `text-base` since mono fonts render wider and the nodes have fixed width
- Tooltip description text: keep the tooltip as-is (serif for descriptive text, small size). It is explanatory content

**learning-journal.tsx:**
- Panel heading "Learning Journal": replace `text-sm font-semibold text-zinc-300` with `type-label text-zinc-300` to make it a mono uppercase tracked heading consistent with the h4 pattern from globals.css
- Entry numbers (1., 2., etc.): change from `text-xs font-medium text-indigo-400/60` to `font-mono text-xs text-indigo-400/60` so numbers render in monospace
- Entry text: keep serif (inherits from body). Journal entries are reflective content that belongs in the reading font
- Empty state text: keep serif, keep subtle styling
  </action>
  <verify>Run `npm run build` -- no errors. Inspect concept-node.tsx for font-mono on labels. Inspect learning-journal.tsx for type-label on heading and font-mono on entry numbers.</verify>
  <done>Concept map node labels render in Courier Prime (mono) with tracking. Learning Journal heading uses type-label class. Entry numbers are monospace. All descriptive/body text remains serif.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. concept-node.tsx label span contains `font-mono` and `tracking-wide`
3. message.tsx role labels use `type-label` class
4. app-header.tsx mode badges contain `font-mono` and `tracking-`
5. confidence-check.tsx assessment badges contain `font-mono` and `uppercase`
6. learning-journal.tsx heading contains `type-label`
</verification>

<success_criteria>
Every UI component explicitly uses the correct font from the serif/mono pairing: conversation content in Libre Baskerville (serif), identifiers and labels in Courier Prime (mono) with visible letter-spacing. The typographic hierarchy creates a clear distinction between reading content and UI chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/09-typography-foundation/09-02-SUMMARY.md`
</output>
